Docker compose does not start the application properly because the compose config and startup scripts are incorrect.

Update the project so a fresh clone works and starts the containers properly.

The API should be reachable on localhost:5000/health and return healthy once Mongo is actually available.
